# invalid: a phase is sequential or reactive, never both
# rule: V9
# id: handler.mixed-phase
# spec: Syntactic restrictions

platform dos
entry a

phase a {
    wait "x"
    always "y" {
        finish
    }
}
