// valid: the backslash form is the literal escape, as in the script language
// spec: Reach
[
  { "type": "machine", "name": "rig", "platform": "dos",
    "devices": { "cdrom0": null },
    "parameters": { "prompt": "\\${HOME} is expanded by the guest" } }
]
