# invalid: a $ reference names a declared property or an rlq fact
# rule: S6
# id: prop.undefined-reference
# spec: Syntactic restrictions

platform dos
property text owner default="${nobody}"
wait "x"
