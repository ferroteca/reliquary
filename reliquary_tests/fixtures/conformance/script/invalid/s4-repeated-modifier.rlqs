# invalid: a repeat is an error, never last-wins
# rule: S4
# cites: no -- the duplicate-modifier diagnostic carries no id (D55)
# spec: Syntactic restrictions

platform dos
wait "x" timeout=1s timeout=2s
