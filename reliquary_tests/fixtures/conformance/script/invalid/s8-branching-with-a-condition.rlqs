# invalid: a branching wait carries no condition of its own
# rule: S8
# id: none
# spec: Syntactic restrictions

platform dos
wait "x" {
    on "a" {
        finish
    }
    on "b" {
        finish
    }
}
