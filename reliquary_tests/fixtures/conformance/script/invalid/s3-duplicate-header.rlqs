# invalid: each header appears at most once
# rule: S3
# cites: no -- the header-cardinality diagnostic carries no id (D55)
# spec: Syntactic restrictions

platform dos
timeout 10s
timeout 20s
wait "x"
