// invalid: a children path never takes a reference - the graph stays static
// spec: Reach
// schema: rejects
// id: ref.not-allowed-here
[
  { "type": "media", "name": "outer", "location": "outer.zip",
    "children": [ { "path": "${which.member}" } ] }
]
